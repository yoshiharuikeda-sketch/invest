# 変更内容 2026-05-26

## 概要
電機・精密（1625.T）および日経半導体ETF（200A）をユニバースから除外。

## 背景
- 証券会社の取り扱い制約により、1625.T（電機・精密）の一般信用デイトレ売建が不可
- 代替として200A（日経半導体ETF）で発注していたが、シグナル計算は1625.Tの相関構造を使用
- シグナル（1625.T）と実際の発注（200A）の値動きが異なる可能性があり、basis riskが発生
- 根本解決として両銘柄をユニバースから除外することに決定

## 変更ファイル

### daily_signal.py
- `JP_TICKERS` から `"1625.T"` を削除（17セクター → 16セクター）
- `JP_NAMES` から `"1625.T": "電機・精密"` を削除
- `JP_CYCLICAL` から `"1625.T"` を削除

### kabu_order.py
- `JP_TICKER_TO_CODE` から `"1625.T": "200A"` のマッピングを削除
- `JP_NAMES` から `"1625.T": "電機・精密"` を削除
- `JP_NAMES` から `"200A.T": "電機・精密代替（日経半導体）"` を削除

### cache_prior.parquet（要対応）
- ローカルの `cache_prior.parquet` を削除済み（Macのクローン環境）
- **Windows環境でも削除が必要**：次回シグナル計算時に16セクターで自動再生成される

```bat
del "G:\My Drive\Claude Code\Invest\cache_prior.parquet"
```

## Windowsでの反映手順

```bat
cd "G:\My Drive\Claude Code\Invest"
git pull
del cache_prior.parquet
```

## 影響
- シグナル計算対象: 17セクター → 16セクター（電機・精密を除外）
- LONGまたはSHORTに選ばれていた場合、その分のポジションはなくなる
- 他の16セクターのシグナル・発注には影響なし
